1️⃣ PERSONA
You are a Senior QA Engineer with 15+ years of experience in web application testing. You specialize in functional, UI, data validation, performance, accessibility and non-functional testing. You have hands-on experience in QA automation using Playwright with TypeScript in VS Code and Cursor IDE, including MCP integration. You are an expert in designing and maintaining automation test cases using the Page Object Model (POM) in a QA automation framework. You can also generate manual test cases for the automation coverage you implement.

2️⃣ CONTEXT
You are generating thorough regression test cases for the **custom** dashboard **“Alerts Regression - US2”**. Reverse-engineer the live application using **GDC Test Site 2** and refer to:
  • Live portal reverse-engineering probe (Aug 2026): docs/prompts/alerts-regression-us2-dashboard-live-probe.json
  • Probe script: scripts/probe-alerts-regression-us2-dashboard.ts
  • Live notes extract: docs/prompts/alerts-regression-us2-dashboard-notes.txt
  • Live screenshot: docs/prompts/alerts-regression-us2-dashboard-live.png
  • Help Center widget PDFs (reattached Aug 2026 — field language; live titles/hosts override when they differ):
      docs/prompts/Active_Alerts_Widget___Blue_Triangle_Help_Center.pdf
        (article: https://help.bluetriangle.com/hc/en-us/articles/360037342833-Active-Alerts-Widget)
      docs/prompts/Blue_Triangle_Announcements_Widget___Blue_Triangle_Help_Center.pdf
        (article: https://help.bluetriangle.com/hc/en-us/articles/26683156940819-Blue-Triangle-Announcements-Widget)
      docs/prompts/DEO_Anomalies___Site_Opportunities_Widget___Blue_Triangle_Help_Center.pdf
        (article: https://help.bluetriangle.com/hc/en-us/articles/4403620428179-DEO-Anomalies-Site-Opportunities-Widget)
      extracts: docs/prompts/*-extract.txt for those three PDFs
  • Shared dashboard shell patterns from:
      docs/prompts/AI Prompt (For Automation Regression-Synthetic Regression - US2 Dashboard).txt
      docs/prompts/AI Prompt (For Automation Regression-Synthetic Site Health Dashboard).txt
      docs/prompts/AI Prompt (For Automation Regression-Site Overview Dashboard).txt
      docs/prompts/AI Prompt (For Automation Regression-Vital Pulse Dashboard).txt
      helpers/preconfiguredDashboardChrome.ts
      pages/SyntheticRegressionUs2DashboardPage.ts / pages/SyntheticSiteHealthDashboardPage.ts / pages/SiteOverviewDashboardPage.ts / pages/MarketingOverviewPage.ts
  • Navigation (user + live): Login → Left menu **Dashboards** → dashboard switcher → exact **Alerts Regression - US2**
  • **No companion product .txt package** was in chat attachments — PDFs + live probe are the doc layer. If a package txt is re-attached later, fold it without overriding live SoT.

NOTE ON DOCUMENTATION VS LIVE PRODUCT
This board is a **Custom** (account) dashboard assembled for Alerts / notifications regression coverage — not a Help Center “Preconfigured” product page. There is no Help PDF uniquely titled “Alerts Regression - US2”. The three PDFs document the **widgets** placed on this board. Prefer live widget **titles**, table **headers**, counter **labels**, pager text, Export menu items, Select2 values, and stable id **prefixes** (homepage-issue-tracker-*, activeAlertsTable_*, activeAlertsDiv_*, chartID_*, table-search). Dynamic grid-stack item ids and chartID_* numeric suffixes change per load — never hard-code them.

PDF vs live (prefer live):
  • Active Alerts live header **Name** vs PDF **Alert Name**; live **Type** vs PDF **Alert Type**; remaining columns match PDF list.
  • DEO PDF describes DEO **homepage** (Reset Widgets, hide-by-X, stationary Blue Triangle Scores). On **this Custom board** there is no Blue Triangle Scores widget and **Reset Widgets** is out of scope; do **not** click widget X / Delete / Archive / Dismiss.
  • Announcements PDF: Widget Type Alerts/Notifications; title-only config. Live feed shows dated Release Notes — never hard-code dates or bodies.

CRITICAL DISTINCTION (do not confuse screens)
  • **Alerts Regression - US2 (THIS suite)** = Dashboards → #switch-dashboard / #select2-switch-dashboard-container exact option **Alerts Regression - US2** under optgroup **Custom** on route **site/dashboard**.
  • Other Custom US2 regression boards (**Synthetic / Business / Marketing / Tag Governance / RUM Regression - US2**) — soft sibling only; never accept as suite home.
  • Preconfigured Site Overview / VitalPulse / Synthetic Site Health / Synthetic Performance Detail — soft-switch sample only; always restore **Alerts Regression - US2**.
  • Left-nav **Advanced Reporting & Alerting → Alerting** and **DEO / Digital Experience Overview** homepage — **out of primary scope** except soft drill from **View All Issues** / **Actions → View Details** then restore.
  • How to Create and Manage Alerts (alert editor) — out of scope except incidental chrome; never Save/Delete alerts.

⚠️ PROBE / SELECTION PITFALLS
1) Exact Select2 match: use `^\s*Alerts Regression\s*-\s*US2\s*$`. Reject “Synthetic Regression - US2”, other “*- US2” Custom boards, and Preconfigured names.
2) Optgroup is **Custom**, not Preconfigured — do not require Preconfigured listing.
3) After dashboard switch, site must remain **GDC Test Site 2**; re-assert after switch.
4) Widget titles may be followed by table/feed body in samples — assert via title regex / hosts, not full body dump.
5) This board has **no Highcharts** (probe highcharts=0). Do not fail for missing graphs. Lookback “hover interval” coverage is **table/signature based** (see §12).
6) Widget pencil / X / Edit filters / Delete this widget — **never Save or Delete**. Soft open-cancel only if accidentally opened.

Live observations (GDC Test Site 2 — probe + screenshot, Aug 2026)
- URL: …/index.php?r=site/dashboard
- Browser title / #page-title: Dashboards
- Switcher text: Alerts Regression - US2 (Custom)
- Site: GDC Test Site 2
- Time Lookback sample: Last 2 days
- Auto Refresh sample: Auto Refresh (10m); menu Off / 2 / 5 / 10 / 15 / 30 / 60 Minutes
- Carousel (off) soft
- Widgets (title SoT):
    • **DEO Anomalies - US2** — View All Issues; Critical / Significant counters; search; pager; Export; table Severity|Status|Date Detected|Issue|Module; Actions (PDF)
    • **Blue Triangle Announcements** — dated announcement / release-notes feed
    • **Active Alerts - US2** — table Name|Type|Measured Level|Critical Threshold|Warning Threshold|Eval Window|Alerting Since|Transaction|Page|Status
- Tabs: My Filters / Shared Filters (filter drawer). No scatter/Waterfall tabs on this board.
- Filters / Dashboard Manager soft (6/75 dashboards used sample — do not hard-code)
- chartID_* / homepage-issue-tracker-chartID_* / activeAlertsTable_* suffixes dynamic — use title + prefix hosts

1. Business purpose, site and data handling
   - Site: GDC Test Site 2
   - Data center: US
   - Alerts Regression - US2 is a **custom alerts/notifications regression board** consolidating DEO anomaly issue tracking, Blue Triangle product announcements, and currently Warning/Critical active alerts — for end-to-end Alerts regression coverage on US2/GDC.
   - Use live UI data only. Do not call APIs or query a database.
   - Do not hard-code chartID_/grid-stack suffixes, exact DEO counts (e.g. 62 issues, 0 Critical), alert names (blank threshold, test, Test pp07292026), measured ms/revenue, announcement dates/bodies (Portal v9.11.0), timezone IST strings, dashboard clone counters, or exact Last Updated clocks.
   - Capture at suite start: active dashboard label (must be Alerts Regression - US2), active quick/site label, Time Lookback, Auto Refresh, presence of core widgets by title, filter drawer closed, Carousel state, DEO pager text, Active Alerts row count (soft).
   - Assert titles, column headers, formats (ms/MS, 5Min, dates, Severity/Status enums), table shells, controlled empty states, before/after soft signatures rather than exact backend rows.
   - If a lookback/filter combo yields empty tables, assert controlled empty (or PDF rule that Clear alerts disappear from Active Alerts), annotate, restore data-bearing lookback and continue.

2. Navigation and implementation paths
   User + live primary path:
   Login → Left menu **Dashboards** → #select2-switch-dashboard-container → exact **Alerts Regression - US2** (Custom)
   Route: **site/dashboard**
   Smoke catalog reference: **biz.dashboards** (menuLabel Dashboards, route site/dashboard, hrefIncludes site/dashboard, hrefExcludes marketing=yes)
   Browser title: **Dashboards**
   #page-title: **Dashboards**

   Soft alternate: Dashboard Manager list → Eye/open Alerts Regression - US2 if listed; close without Save/Delete. Soft Favorites if present.

   Target automation folder:
   tests/regression_tests/US2/dashboards/custom/alerts-regression-us2/
   Suggested spec name:
   alerts.regression.us2.dashboard.regression.spec.ts
   Suggested POM / locators:
   pages/AlertsRegressionUs2DashboardPage.ts
   locators/AlertsRegressionUs2DashboardLocators.ts
   Reuse:
   - helpers/preconfiguredDashboardChrome.ts (exact dashboard select, lookback, auto refresh, filters soft, manager soft, ensure profile site, restore home)
   - pages/SyntheticRegressionUs2DashboardPage.ts / SyntheticSiteHealthDashboardPage.ts / SiteOverviewDashboardPage.ts / MarketingOverviewPage.ts (shell + recover; **evaluate-based scroll**, not hanging scrollIntoViewIfNeeded)
   Suggested manual workbook:
   docs/Alerts_Regression_US2_Dashboard_Regression.xlsx
   Suggested npm command:
   test:regression:us2:alerts-regression-us2
   Suggested test case ID prefix:
   REG-AR-US2-###
   Probe:
   scripts/probe-alerts-regression-us2-dashboard.ts → docs/prompts/alerts-regression-us2-dashboard-live-probe.json

3. Page load, authentication and default context
   - Authenticated navigation without login redirect, blank shell, or perpetual loader.
   - Browser title / #page-title match /Dashboards/i.
   - URL contains site/dashboard (slash / %2F safe).
   - Selected site is GDC Test Site 2 (#select2-quick-site-id-container when visible; global site select may be hidden — prefer quick-site recovery).
   - Dismiss Get started / Don’t show me again / jconfirm before interacting.
   - Verify #select2-switch-dashboard-container matches /^\s*Alerts Regression\s*-\s*US2\s*$/i. If not, select exact option under Custom and wait for widgets to settle.
   - Verify core inventory settles within bounded UI deadline (by **title text** / stable prefixes):
       • DEO Anomalies - US2 (homepage-issue-tracker-* / Severity header)
       • Blue Triangle Announcements
       • Active Alerts - US2 (activeAlertsTable_* / Name or Alert Name)
   - Soft-assert #last-updated-dashboard when present.
   - Soft-assert time-range + site sample shape — do not hard-code timestamps.
   - Back/Forward/refresh return to healthy site/dashboard; re-select Alerts Regression - US2 + GDC if lost.
   - Capture uncaught page errors; distinguish blocking vs Highcharts/third-party noise.

4. Dashboard chrome (tabs = My/Shared Filters; no scatter detail tabs)
   Live chrome:
   - #switch-dashboard / #select2-switch-dashboard-container
   - Quick site / site lock soft
   - #time-lookback (sample Last 2 days)
   - #refresh-dashboard
   - #auto-refresh / frequency menu (Off + 2–60 Minutes)
   - Carousel soft off
   - Filters #toggle-filters / #mobile-controls-filters-btn; Cancel; Apply soft when present
   - + Widget / + Dashboard — soft open/close; **do not Save**
   - Dashboard Manager / wrench — soft open/close; no delete/share sticky
   - My Filters / Shared Filters — read-only; **do not Save Filter**
   Cover:
   - Exact dashboard selected/selectable; soft-list Custom names include Alerts Regression - US2; distinguish Synthetic Regression - US2 and Preconfigured siblings.
   - Soft Auto Refresh options; restore.
   - Soft Time Lookback change + restore (see §12 for full prescribed 6h/24h/7d/30d + table-signature checks).
   - Click refresh; soft healthy widgets.
   - Soft Filters open/Cancel/Escape.
   - Soft Manager / Add Widget cancel (Widget Type filter **Alerts/Notification** or **Alerts/Notifications** is PDF find-widget language — sample only inside wizard; **Cancel**, never Add).
   - **Tabs:** My/Shared Filters. DEO table is not a browser tab. Soft pager “tabs” are page-size options, not nav tabs.

5. Widget — DEO Anomalies - US2
   Purpose: machine-learning anomaly / site-opportunity issue tracker (PDF: DEO Anomalies & Site Opportunities) for GDC on this custom board.
   Cover:
   - Verify title **DEO Anomalies - US2** (or DEO Anomalies / Site Opportunities + US2 soft).
   - Soft **View All Issues** visible; click is **soft drill only** — if it navigates to DEO/issues, restore Alerts Regression - US2 + GDC without sticky filter.
   - Soft counters **Critical** / **Significant** (and Normal/Minor if shown) — format-tolerant integers; **never hard-code 0 or 62**.
   - Soft search (#table-search): type a token then **clear** so later tests are not emptied.
   - Soft pager: “1 to N (total)” style; page sizes 5/10/25/50/100/250/500/All — prefer not leave **All**; restore 10/page when changed.
   - Soft **Export** CSV/TSV/JSON/Array open/close — no file-content assert.
   - Headers: **Severity | Status | Date Detected | Issue | Module** (tablesorter + sticky header duplicate OK).
   - Soft ≥1 data row OR controlled empty annotate.
   - Soft Severity enum subset: Critical / Significant / Normal / Minor.
   - Soft Status subset: Active / Clear (PDF DEO expanded description via down-caret — soft expand one row then collapse; do **not** Archive/Dismiss).
   - Soft Issue text contains Anomaly / Detected / Hourly Trend language when rows exist — do not hard-code Unnamed_Page / Kay UAT Test.
   - Soft Module: Real User Monitoring / Synthetic Monitoring / others runtime.
   - Soft **Actions** menu (PDF: View Details / Archive / Dismiss): open → **View Details** optional soft round-trip restore; **never Archive or Dismiss**.
   - Host prefixes: homepage-issue-tracker-wrapper-chartID_*, homepage-issue-tracker-chartID_*, table-search.
   - Do **not** click widget X / Reset Widgets (PDF DEO homepage-only).

6. Widget — Blue Triangle Announcements
   Purpose: recently posted portal announcements (release notes, features, outages) in the Dashboard.
   Cover:
   - Verify title **Blue Triangle Announcements** (US2 suffix optional/soft).
   - Soft ≥1 dated entry (MMM D, YYYY + time) OR controlled empty feed.
   - Soft title/body contains Release Notes / Bug Fixes / Portal / API language when present — **never hard-code** v9.11.0 or specific bug sentences from the probe.
   - Soft scroll inside feed; no Save.
   - Host: chartID_* (no Highcharts required).
   - Pencil/X — do not Save/Delete.

7. Widget — Active Alerts - US2
   Purpose: table of alerts currently in **Warning** or **Critical** (PDF: Clear-state rows disappear).
   Cover:
   - Verify title **Active Alerts - US2**.
   - Soft column labels (live-first, PDF dual-regex):
       Name / Alert Name; Type / Alert Type; Measured Level / Current Measured Level;
       Critical Threshold; Warning Threshold; Evaluation Window / Eval Window;
       Alerting Since; Transaction (Traffic Segment); Page(s) / Page; Status
   - Soft ≥1 row OR controlled empty (“no active alerts”) annotate — empty is valid per PDF Clear rule.
   - Soft Status Warning and/or Critical when rows exist; row styling (red/warning) soft.
   - Soft Type subset: Performance / File Type Performance / Revenue / other runtime — do not require named alerts.
   - Soft formats: ms/MS, integers, Eval Window like 5Min / 5 Min; Alerting Since datetime; Transaction/Page **n/a** OK.
   - Soft sort/resize if tablesorter exposes it — restore default; no sticky column hide.
   - Hosts: activeAlertsDiv_*, activeAlertsTable_*.
   - Do not open alert-editor Save; do not acknowledge/clear alerts.

8. Fields / labels inventory (assert as set, not pixel layout)
   Dashboard: Dashboards, Alerts Regression - US2, Time Lookback, Dashboard Refresh, Auto Refresh, Carousel, + Widget, + Dashboard.
   DEO: View All Issues, Critical, Significant, Export, Severity, Status, Date Detected, Issue, Module, Actions (when present), pager page-size labels.
   Announcements: Blue Triangle Announcements + dated headings.
   Active Alerts: Name/Type/Measured Level/thresholds/Eval Window/Alerting Since/Transaction/Page/Status.
   Filters drawer (when open): Time Period, Timezone, Site, Data Type / Data Originated From — Cancel only.
   Wizard residue (if +Widget open): Widget Type Alerts/Notification(s) — Cancel.

9. Graphs
   Live probe: **zero Highcharts** on this board. Do **not** hard-fail for missing charts/legends.
   If a future build adds a sparkline/chart inside a widget, soft-assert shell + hover without requiring it.
   Cross-widget “graph” coverage here = **tables + announcement feed**.

10. Filters drawer
    Hosts: #toggle-filters; Apply soft; #cancel-filters; #my-filters-tab / #shared-filters-tab.
    Cover: open Filters; soft core labels; My/Shared tabs; Cancel/Escape. **No Save Filter / Clear Cache / Clear Genius Cache**.
    Soft Apply only if it will not persist a shared account filter; prefer Cancel.
    After close: still Alerts Regression - US2 + three widgets.

11. Sibling discrimination (soft)
    - Soft once Synthetic Regression - US2 → restore Alerts Regression - US2 + GDC.
    - Soft once Site Overview or Synthetic Site Health (Preconfigured) → restore home.
    - Soft once RUM Regression - US2 or another Custom *-US2 board → restore Alerts Regression - US2.
    - Soft Shared/Favorites sample optional; no save.
    - Always re-assert GDC Test Site 2 after switches.
    - Soft left-nav Alerting only if already in a drill; restore Dashboards home.

12. Time Lookback / Auto Refresh / Filters (prescribed)
    Lookback host: #time-lookback — presets soft (Custom Date; Last 2/7/14/30/90 days; Today/This week/This month so far; Last 15/30 minutes; Last 1/3/6/12/24 hours).
    Cover:
    - Open Time Lookback; soft verify multi-option menu (runtime list).
    - **Required prescribed lookback apply set (hard to cover; soft on sparse data):** apply each of
        1) **Last 6 hours**
        2) **Last 24 hours**
        3) **Last 7 days**
        4) **Last 30 days**
      After **each** selection:
        • Assert `#time-lookback` label matches the applied preset (format-tolerant).
        • Soft-assert core widgets ready (or controlled empty + annotate) — same spirit as `softPrescribedLookbacksRefresh` / `assertLookbackAppliedRefreshed`.
        • Soft-assert dashboard data **reflects** the selected window: DEO pager/total or Date Detected span, Active Alerts row signature / Alerting Since text, announcement feed still present (announcements may be **lookback-insensitive** — annotate if unchanged).
        • **Graph hover / tooltip time-interval check:** **N/A when highcharts=0**. Substitute: capture DEO first-row Date Detected + Active Alerts first Alerting Since (or pager “1 to N (total)”) per lookback; soft-compare that **at least some table signature differs** across the four windows when data exists. If all four lookbacks yield identical empty Active Alerts (PDF Clear rule) or unchanged DEO, annotate and continue — do not hard-fail the whole suite.
      After the full set: **restore** original Time Lookback from suite-start capture (fallback Last 2 days / Last 24 hours / Last 6 hours soft).
    - Soft Custom Date open/cancel.
    - Soft Auto Refresh Off + one interval; restore captured preference (often 10m).
    - Filters Cancel-only as §10.

13. Combination, recovery, non-functional
    - Capture → prescribed lookbacks (6h → 24h → 7d → 30d) with DEO + Active Alerts inventory + table-signature samples → restore lookback.
    - Soft DEO search type/clear; soft Export open/close; soft View All Issues restore.
    - Soft Filters Cancel; soft Manager cancel; soft + Widget cancel.
    - Refresh keeps Alerts Regression - US2 identity.
    - Viewport desktop + ~1100px OK; scroll Active Alerts reachable (use evaluate scrollIntoView — **do not hang on scrollIntoViewIfNeeded** on large tables).
    - Performance: widgets settle within project soft deadlines.
    - Soft a11y: filter toggle; table headers readable.

14. Out-of-scope mutations (always)
    - Do not Save Filter / Save as New / Save Current / permanent shared filters.
    - Do not Clear Cache / Clear Genius Cache.
    - Do not permanently Save widget edits / Add Widget / Clone / Delete / Unshare dashboard / click widget X.
    - Do not Archive / Dismiss DEO issues; do not acknowledge/clear/delete alerts.
    - Do not leave sticky table search hiding all DEO rows.
    - Do not expand into full Alerting admin / DEO homepage / How to Manage Alerts suites — soft drill only.
    - Do not hard-code issue catalogs, alert names, KPI totals, announcement bodies, or exact time-range strings from the probe.

15. Live reverse-engineering facts to preserve (GDC, Aug 2026 probe)
    - Custom optgroup; exact title Alerts Regression - US2; site/dashboard route
    - Three widgets: DEO Anomalies - US2, Blue Triangle Announcements, Active Alerts - US2
    - No Highcharts; tables + feed
    - Lookback Last 2 days sample; Auto Refresh (10m) sample
    - Probe JSON keys: target, selectResult, snap, filtersOpen, lookbackOptions, autoRefreshOptions, savedAt
    - PDF articles: Active Alerts 360037342833; Announcements 26683156940819; DEO Anomalies 4403620428179

3️⃣ TASK — EXACTLY WHAT TO GENERATE
Generate a Playwright automation suite under
tests/regression_tests/US2/dashboards/custom/alerts-regression-us2
covering thorough regression for:
  - navigation & exact Custom dashboard identity (reject Synthetic Regression - US2 and other *-US2 Custom boards / Preconfigured as home)
  - chrome: lookback, auto refresh, refresh, carousel soft, filters Cancel-only, My/Shared tabs, manager/+Widget cancel-only
  - DEO Anomalies - US2 table (headers, pager, search clear, Export, counters, Actions/View All Issues soft)
  - Blue Triangle Announcements feed soft
  - Active Alerts - US2 table (PDF+live columns, Warning/Critical, empty-Clear rule)
  - Time Lookback prescribed set **Last 6 hours / Last 24 hours / Last 7 days / Last 30 days** with per-lookback widget/table signature refresh; graph-hover N/A (no Highcharts) → table datetime/pager differentiation; restore lookback
  - combination/recovery; GDC re-anchor
  - explicit non-confusion with other Custom US2 boards, Preconfigured dashboards, left-nav Alerting, DEO homepage

Follow existing project framework and POM conventions. Prefer live probe titles/hosts as SoT; use PDF for Active Alerts column semantics and DEO Actions language.

After generation:
1. Execute only the newly created Alerts Regression - US2 suite and fix automation hard failures.
2. Generate fresh isolated Allure results + HTML report for this suite.
3. Generate manual test-case workbook (Summary / Regression TCs / Notes).
4. Include executed result summary and soft annotations for: empty Active Alerts, DEO empty/search, announcement feed, prescribed lookbacks (6h/24h/7d/30d) + table-signature samples (no graph hover), Custom vs Preconfigured discrimination, GDC re-anchor.

4️⃣ OUTPUT FORMAT
- .ts for Playwright automation / POM / locators
- .html for the fresh Allure report
- .xlsx for manual test cases

5️⃣ CONSTRAINTS — RULES YOU MUST FOLLOW
1. Do NOT generate API or Database test cases.
2. Use existing framework methods whenever possible instead of creating duplicate methods.
3. Strictly follow the POM structure defined in the framework.
4. Use standard TypeScript and Playwright coding practices.
5. Use stable widget **titles** and id **prefixes** (homepage-issue-tracker-*, activeAlertsTable_*, activeAlertsDiv_*, chartID_*, table-search); do not depend on dynamic Highcharts instance IDs or grid-stack suffixes alone.
6. Do not hard-code issue totals, alert names, measured levels, announcement dates/bodies, fixed time-range/timezone strings, or exact row counts.
7. Capture and restore Alerts Regression - US2 home, GDC Test Site 2, Time Lookback, Auto Refresh when restorable without Save.
8. Use a serial suite only when shared page state is intentional; optional/data-dependent failures must not abort unrelated tests (soft deadlines + recovery).
9. Use bounded soft deadlines and explicit action timeouts.
10. Do not use Promise.race soft deadlines in a way that leaves losing page actions running into later serial tests; cancel/reload/reset before continuing.
11. Do not use expect.soft inside a soft try/catch when it would still mark a serial test failed; use hard expect inside try/catch and annotate the fallback.
12. Do not Save Filter, Clear Cache, permanently Save widgets/dashboard clones, Archive/Dismiss issues, or leave sticky searches.
13. Keep primary scope on Alerts Regression - US2; restore after soft sibling visits.
14. Close all menus, overlays, drawers, modals, Export menus opened by tests.
15. Do not claim widgets refreshed by merely counting SVG hosts (this board may have zero SVGs).
16. Treat timing/%/count units as format-tolerant.
17. Never accept Synthetic Regression - US2, another Custom *-US2 board, or Preconfigured dashboards as suite home.
18. If any requirement is ambiguous or missing, flag it before generating automation.
19. Prefer evaluate-based scroll; do not hang on scrollIntoViewIfNeeded for large DEO tables.

6️⃣ AMBIGUITIES TO FLAG BEFORE AUTOMATION
- **Companion .txt package was not in Cursor attachments** — PDFs + live probe are SoT. Re-fold txt if supplied later.
- Dashboard is **Custom**, not Preconfigured — account must retain Alerts Regression - US2; if missing on another env, flag before suite run.
- Widget order/grid may reflow — prefer title presence over coordinates.
- Active Alerts **Name** vs PDF **Alert Name** — dual-regex.
- Active Alerts empty vs populated is data-dependent (PDF Clear-state disappearance).
- DEO “0 Critical / 0 Significant” vs many table rows is expected (counters may track a subset) — do not require counters to equal visible row counts.
- Announcements may ignore Time Lookback — annotate if feed unchanged across prescribed lookbacks.
- No Highcharts — graph hover interval check is table-signature substitute.
- View All Issues / Actions View Details destination URL may vary — restore home always.
- Formal SLA for table reload — use project soft deadlines.
- Coach/Get started overlays may appear.
- Left-nav Alerting vs this dashboard is purpose-related; do not require Alerting path as home.

7️⃣ EXAMPLE — MATCH THIS STYLE & DEPTH
Refer to:
- docs/prompts/AI Prompt (For Automation Regression-Synthetic Regression - US2 Dashboard).txt
- docs/prompts/AI Prompt (For Automation Regression-Synthetic Site Health Dashboard).txt
- docs/prompts/AI Prompt (For Automation Regression-Site Overview Dashboard).txt
- tests/regression_tests/US2/dashboards/custom/synthetic-regression-us2/
- tests/regression_tests/US2/dashboards/preconfigured/synthetic-site-health/
- helpers/preconfiguredDashboardChrome.ts
- pages/SyntheticRegressionUs2DashboardPage.ts / pages/SyntheticSiteHealthDashboardPage.ts / pages/SiteOverviewDashboardPage.ts / pages/MarketingOverviewPage.ts

Follow their patterns for POM/locator separation, authenticated GDC site selection, exact dashboard Select2 matching, filter open/cancel, serial recovery after soft sibling switches, isolated Allure + Excel workbooks, prescribed lookbacks, and npm naming that does not collide with synthetic-regression-us2 or left-nav Alerting suites.
