1️⃣ PERSONA
You are a Senior QA Engineer with 15+ years of experience in web application testing. You specialize in functional, UI, data validation, performance, accessibility and non-functional testing. You have hands-on experience in QA automation using Playwright with TypeScript in VS Code and Cursor IDE, including MCP integration. You are an expert in designing and maintaining automation test cases using the Page Object Model (POM) in a QA automation framework. You can also generate manual test cases for the automation coverage you implement.

2️⃣ CONTEXT
You are generating thorough regression test cases for the preconfigured **RUM Performance Detail** dashboard (Help title: “The RUM Performance Detail Dashboard”). Reverse-engineer the live application using **GDC Test Site 2** and refer to:
  • Help Center PDF: “The RUM Performance Detail Dashboard - Blue Triangle Help Center.pdf”
    (article: https://help.bluetriangle.com/hc/en-us/articles/360033246414-The-RUM-Performance-Detail-Dashboard)
  • Live portal reverse-engineering probe (Aug 2026): docs/prompts/rum-performance-detail-dashboard-live-probe.json
  • Probe script: scripts/probe-rum-performance-detail-dashboard.ts
  • PDF extract notes: docs/prompts/rum-performance-detail-pdf-extract.txt
  • Optional live screenshot: docs/prompts/rum-performance-detail-dashboard-live.png
  • Package folder: AI Agents …/dashboards/preconfigured/rum-performance-detail/ (PDF package materials; **no companion product .txt** beyond this AI Prompt)
  • Shared dashboard shell patterns from:
      docs/prompts/AI Prompt (For Automation Regression-Site Overview Dashboard).txt
      docs/prompts/AI Prompt (For Automation Regression-Vital Pulse Dashboard).txt
      docs/prompts/AI Prompt (For Automation Regression-Synthetic Site Health Dashboard).txt
      docs/prompts/AI Prompt (For Automation Regression-Traffic Source and Medium Dashboard).txt
      pages/SiteOverviewDashboardPage.ts / pages/VitalPulseDashboardPage.ts / pages/SyntheticSiteHealthDashboardPage.ts / pages/TrafficSourceMediumDashboardPage.ts
      helpers/preconfiguredDashboardChrome.ts / pages/MarketingOverviewPage.ts

NOTE ON DOCUMENTATION VS LIVE PRODUCT
The PDF is short (~6 pages) covering Overview, navigation (Dashboards dropdown + gear/eye), three conceptual widget groups (Bar Graph & Details, Performance Graph, Daily Averages), Auto Refresh frequency bounds, and Filters.  
PDF Table of Contents contains a stale/wrong subsection label (“How to Find the **Synthetic** Performance Detail Dashboard”) — ignore that TOC typo; body section is correctly “How to Find the RUM Performance Detail Dashboard”.

The PDF does **not** document every live chrome control (Carousel, + Widget / + Dashboard, My Filters / Shared Filters, Daily Averages page-size menus, Export CSV/TSV/JSON/Array, “All Pages” / “All Pages Average” / “Deselect All Metrics” affordances, full Details metric card catalog, Performance Graph full legend set, Test Override metric on some sites, unit suffixes like (Sec) on daily columns, quick-site vs graph site subtitle mismatch, etc.). Prefer live control ids, widget **titles**, metric **labels**, table **headers**, legend series names, pagination/export labels, and Select2 values when the PDF is incomplete or older than the product UI.

CRITICAL DISTINCTION (do not confuse screens)
  • **RUM Performance Detail Dashboard (THIS suite)** = Preconfigured dashboard on route **site/dashboard**, menu **Dashboards**, option **RUM Performance Detail** under optgroup **Preconfigured** in #switch-dashboard / #select2-switch-dashboard-container.
    - Exact option text: **RUM Performance Detail** (live). Soft-tolerate rare **RUM Performance Detail Dashboard** if product renames; never partial-match “Performance Detail” alone (hits Synthetic / Native App siblings).
  • **Monitoring → Real User Browser → Performance Detail** (Monitoring page, help article 42336918655763) — **different path**; has scatterplots, session lookup entry points, Performance Breakdown waterfall, page-view picker with “Select All” limits, View/Hide Filters — **out of primary scope**. Existing suite:
      tests/regression_tests/US2/monitoring/real-user-browser/performance-detail/rum.performance-detail.browser.regression.spec.ts
      npm: test:regression:us2:rum-pd
    Soft-relate only: product purpose overlaps; do **not** navigate that menu path as suite home, do **not** re-implement that suite’s scatter/waterfall/session-lookup cases here.
  • **Synthetic Performance Detail** preconfigured dashboard — sibling only; soft-switch then restore **RUM Performance Detail**. Never accept as suite home.
  • **Native App Performance Detail** / Native App Detailed Metrics-* — sibling only; restore home.
  • **RUM Performance Overview**, Aggregate Waterfall, Bounce & Exit, Errors Explorer, Session Lookup, Performance Budgets — Monitoring RUM modules; **out of primary scope**.
  • Site Overview, VitalPulse, Synthetic Site Health, Traffic Source and Medium — sibling preconfigured only; soft-switch then always restore **RUM Performance Detail** + GDC Test Site 2.
  • Digital Experience Overview (DXO) / Marketing Overview — **out of primary scope**.

⚠️ PROBE / SELECTION PITFALLS
1) Exact Select2 match: Use `^\s*RUM Performance Detail\s*$`. Reject options containing Synthetic, Native App, or Overview. Partial “Performance Detail” is unsafe.
2) Quick site: after dashboard switch the graph/site subtitle may briefly show another site (probe first paint sample: bar/range subtitle “Demo eCommerce Global” while #select2-quick-site-id-container already **GDC Test Site 2**). Suite must **always re-assert GDC Test Site 2** after dashboard switch; soft-refresh or re-apply lookback if widgets still subtitle a non-profile site.
3) Existing npm `test:regression:us2:rum-pd` is **Monitoring** Performance Detail — **do not reuse** that script name for this preconfigured suite.

1. Business purpose, site and data handling
   - Site: GDC Test Site 2
   - Data center: US
   - RUM Performance Detail dashboard is a **preconfigured real-time performance analytics view**: high-level page metric bar chart + Detail metric cards; a multi-series Page Timings Over Time performance graph; and a Daily Averages matrix of the selected metric by page name × date (+ 30 Day Average). PDF: “preconfigured view of all the important information from the RUM Performance Detail page in real-time.”
   - Card clicks drive the Bar Graph series **and** which metric Daily Averages displays (PDF). Performance Graph gear + legend control which timing series are plotted.
   - Metric value formats are unit- and site-dependent (seconds `s`/`S`, milliseconds `ms`/`MS`, unitless CLS, K/M page-view suffixes, integer Errors). Do **not** hard-require dollar currency for this board (performance-first; no Sitewide Totals revenue block).
   - Use live UI data only. Do not call APIs or query a database.
   - Do not hard-code Highcharts internal ids (highcharts-*), chartID_* suffixes, exact onload/LCP/INP values, exact page names (PDP, Category, Homepage, …), exact pager “1 to 10 (25)”, exact timezone strings, or dashboard clone counters “0/75”.
   - Capture at suite start: active dashboard label (must be RUM Performance Detail), active quick site, Time Lookback label, Auto Refresh label, presence of Bar Graph + Details + Performance Graph + Daily Averages, filter drawer closed/open, Carousel state, selected Details metric (often Onload / Page Views).
   - Assert titles, metric labels, formats, graph shells, table shells, pagination chrome, controlled empty states, before/after soft signatures rather than exact backend values.
   - If a filter/time combo yields empty Daily Averages or zero-series graphs, assert controlled empty, annotate, restore a data-bearing context and continue.
   - Widget layout (PDF implied: Bar|Details top, Performance Graph middle, Daily Averages bottom) may reflow with grid — prefer **title / body text presence** and stable host ids when present.

2. Navigation and implementation paths
   PDF + Live primary path (prefer this):
   Menu > **Dashboards**
   Route (observed): **site/dashboard**
   Smoke catalog reference: **biz.dashboards** (menuLabel Dashboards, route site/dashboard, hrefIncludes site/dashboard, hrefExcludes marketing=yes)
   Browser title observed: **Dashboards**
   #page-title observed: **Dashboards**
   After load, dashboard switcher #select2-switch-dashboard-container must show / resolve to **RUM Performance Detail** under optgroup **Preconfigured**. First paint may still be Site Overview or another default — **always select exact option text**.

   Alternate discovery paths (PDF — soft coverage):
   1) Upper-left #switch-dashboard / Select2 → select **RUM Performance Detail**.
   2) Gear / wrench **Dashboard Manager** (#dashboard-manager / #dashboard-settings-toggle) → list → soft **Eye**/open RUM Performance Detail if listed; close without Save/Delete.
   Do **not** delete preconfigured dashboards. Do **not** leave manager Save sticky changes.

   Target automation folder:
   tests/regression_tests/US2/dashboards/preconfigured/rum-performance-detail/
   Suggested spec name:
   rum.performance.detail.dashboard.regression.spec.ts
   Suggested POM / locators:
   pages/RumPerformanceDetailDashboardPage.ts
   locators/RumPerformanceDetailDashboardLocators.ts
   Reuse / extend patterns from:
   - helpers/preconfiguredDashboardChrome.ts
   - pages/SiteOverviewDashboardPage.ts / VitalPulseDashboardPage.ts / SyntheticSiteHealthDashboardPage.ts / TrafficSourceMediumDashboardPage.ts (same site/dashboard shell)
   - pages/MarketingOverviewPage.ts (lookback / auto refresh / filters / recoverPage)
   - Monitoring RUM Performance Detail POMs **only** for metric naming ideas / soft cross-checks — **not** as navigation home
   Suggested manual test workbook:
   docs/RUM_Performance_Detail_Dashboard_Regression.xlsx
   Suggested npm command (**must differ from Monitoring rum-pd**):
   test:regression:us2:rum-performance-detail-dashboard
   Probe script (reference):
   scripts/probe-rum-performance-detail-dashboard.ts → docs/prompts/rum-performance-detail-dashboard-live-probe.json

3. Page load, authentication and default context
   - Verify authenticated navigation succeeds without login redirect, blocking error, blank shell or perpetual loader.
   - Verify browser title matches /Dashboards/i.
   - Verify #page-title matches /Dashboards/i.
   - Verify URL contains site/dashboard (slash / %2F safe patterns).
   - Verify selected portal site is GDC Test Site 2 (#select2-quick-site-id-container when visible; #select2-site-id-container may be **hidden** on this shell — prefer quick-site recovery).
   - Dismiss Get started / Don’t show me again / jconfirm overlays before interacting.
   - Verify #select2-switch-dashboard-container matches /^\s*RUM Performance Detail\s*$/i. If not, select exact option and wait for widgets to settle.
   - Verify core PDF/live widgets settle within a bounded UI deadline (by text / stable host ids — not dynamic highcharts-* alone):
       • **Bar Graph** — title/body contain Bar Graph; chart host **#multi-step-bar-graph** (probe)
       • **Details** — body/title contain Details; subtitle soft **all selected pages (avg)**; card region with metric labels; host sample **#page-onload-card** / metric cards
       • **Performance Graph** — **Page Timings Over Time for All Matched Pages** (PDF + live); host **#perfGraph-page-timings-graph** / **#page-timings-graph**
       • **Daily Averages** — title/body **Daily Averages** + selected metric name (e.g. Onload); table host **#table-for-average-performance-chart** / **#table-metric** patterns
   - Soft-assert time-range on Bar Graph / Performance Graph headers (sample: “YYYY-MM-DD HH:mm to YYYY-MM-DD HH:mm, IST (+05:30)” + site name) — do not hard-code timestamps or IST offset.
   - Soft-assert #last-updated-dashboard when present.
   - Verify browser Back/Forward and refresh return to a healthy site/dashboard context; re-select RUM Performance Detail + GDC if lost.
   - Capture uncaught page errors; distinguish app-blocking errors from Highcharts/third-party noise.

4. Dashboard chrome (tabs = My/Shared Filters + soft concept; no multi-campaign page tabs)
   Live chrome hosts/controls observed on GDC RUM Performance Detail (samples — not permanent values):
   - Dashboard switcher: #switch-dashboard / #select2-switch-dashboard-container (text: **RUM Performance Detail**)
   - Quick site: #select2-quick-site-id-container (sample: GDC Test Site 2)
   - Time Lookback: #time-lookback (probe sample: **Last 1 hour**; changeable)
   - Dashboard Refresh: #refresh-dashboard
   - Auto Refresh: #auto-refresh / #auto-refresh-frequency / #auto-refresh-btn-group
     Live options (probe): **Off**, **2 Minutes**, **5 Minutes**, **10 Minutes**, **15 Minutes**, **30 Minutes**, **60 Minutes**
     Matches PDF: “as low as every 2 minutes up to once every hour.” Soft-assert subset; sample button **Auto Refresh (5m)**.
   - Carousel when present (sample: Carousel (off)) — soft open/close; do not leave cycling
   - Filters: #toggle-filters / #mobile-controls-filters-btn; Apply soft; #cancel-filters
   - #dashboard-page-controls wrapper
   - + Widget / + Dashboard — soft open/close only; **do not Save**
   - Dashboard Manager / wrench — soft open/close; no delete/share sticky
   - My Filters / Shared Filters (#my-filters-tab / #shared-filters-tab) — read-only; **do not Save Filter**
   - #last-updated-dashboard soft

   Cover:
   - Verify switcher attached; RUM Performance Detail selected or selectable by **exact** option text.
   - Soft-list Preconfigured names (RUM Performance Detail present; distinguish Synthetic Performance Detail / Native App Performance Detail); restore if sampled others.
   - Soft Auto Refresh menu options (include 2m and 60m soft); restore.
   - Soft Time Lookback change + restore.
   - Click #refresh-dashboard; soft healthy widgets.
   - Soft Filters open/Cancel/Escape.
   - Soft Dashboard Manager; soft Add Widget cancel.
   - **Tabs:** My Filters / Shared Filters only for page-level tabs. Daily Averages uses **pagination** (not primary nav tabs). Metric card selection is interaction state, not browser tabs.

5. Widget — Bar Graph & Details (PDF “Bar Graph & Details”; live multi-step bar + metric cards)
   Purpose (PDF): high-level overview of metrics during the selected time period. Clicking metric cards on the right updates the bar graph on the left **and** the Daily Averages displayed metric.

   5a. Bar Graph (left)
   Live observations:
   - Body/title contains **Bar Graph** and current metric name (sample **Onload**).
   - Time-range + site subtitle on widget.
   - Highcharts bar / multi-step bar; host **#multi-step-bar-graph**.
   - Soft category / “All Pages” affordance on chart (“All Pages” / axis labels 0s… scales).
   - Soft button texts seen in probe chrome: **All Pages Average**, **Deselect All Metrics** (bind when visible; soft open/apply cancel).

   Cover:
   - Verify Bar Graph region visible after selection (text / multi-step-bar-graph host).
   - Soft-assert chart shell sized (probe ~608×440) when hasChart.
   - Soft hover/tooltip when series exist.
   - Soft **All Pages** / category interaction if clickable without permanent sticky filter — restore selection if needed.
   - After metric card click (below): soft assert Bar Graph title/body metric name can change (e.g. Onload → Page Views / LCP); soft signature change.

   5b. Details cards (right)
   Live container text pattern: **Details** + **all selected pages (avg)** (casing may vary).
   Live metric card labels observed on GDC / Demo (assert core subset with flexible ALL CAPS / Title Case regex; site may add extras):
   - **PAGE VIEWS**
   - **ONLOAD**
   - **LARGEST CONTENTFUL PAINT**
   - **INP**
   - **CUMULATIVE LAYOUT SHIFT**
   - **TOTAL BLOCKING TIME**
   - **FIRST BYTE**
   - **TCP**
   - **DNS**
   - **DOM DURATION**
   - **SSL**
   - **REDIRECT**
   - **BASE PAGE**
   - **TIME TO DOM INTERACTIVE**
   - **TIME TO DOM CONTENT LOADED**
   - **TIME TO INTERACTIVE** (may appear; soft)
   - **FIRST CONTENTFUL PAINT**
   - **ERRORS AND CRASHES**
   Soft extra: **TEST OVERRIDE** (observed on GDC filter/settled state) — do not fail if absent; do not hard-require if present on only some sites.
   Value samples (illustrative only — **never hard-code**): 8.75K, 3.82 S, 3.76 S, 225 MS, 0.263, 1.33 S, 680 ms, …
   Soft card tooltips: probe showed “Total number of page hits…” on PAGE VIEWS hover — soft accept description tooltips when shown.

   Cover:
   - Verify Details region + at least a core subset of metric labels (Page Views, Onload/LCP/INP/CLS soft set as required core of ≥5 labels).
   - Soft-assert value formats: K/M numbers; time with s/ms; unitless shift; integer-like errors.
   - **Required metric card interactions:** click **PAGE VIEWS**, click **ONLOAD**, click one CWV card (LCP or INP or CLS) — soft assert Bar Graph and/or Daily Averages header metric text can update per PDF; restore to Onload (or original) if restorable.
   - Soft host #page-onload-card when present.
   - Do not require every listed card if a site build hides a metric; annotate missing non-core cards.

6. Widget — Performance Graph (PDF + live mid board)
   Purpose (PDF): “Page Timings Over Time for All Matched Pages” over selected period. Gear controls included metrics; legend toggles series visibility.

   Live observations:
   - Body contains **Performance Graph** and **Page Timings Over Time for All Matched Pages**.
   - Host: **#perfGraph-page-timings-graph** (also soft #page-timings-graph).
   - Time-range + site subtitle parallel Bar Graph.
   - Legend / series (probe; assert multi-series presence, not exact order/all):
       Page Views · Onload · 1stByte · DNS · TCPCon · DOM Duration · SSL · Redirect · Base Page ·
       Time to DOM Interactive · Time to DOM Content Loaded · JS Errors (Sampled) · Time to Interactive ·
       First Contentful Paint · Largest Contentful Paint · Total Blocking Time · Cumulative Layout Shift · INP
     Soft extras: **TEST OVERRIDE** on some site/data combinations.
   - Dual Y axes soft (seconds vs counts) — do not hard-assert axis tick values.

   Cover:
   - Verify Performance Graph + Page Timings Over Time title/body after selection.
   - Soft-assert Highcharts container sized (probe ~1250×400).
   - Soft **legend toggle**: click one series name (e.g. Onload or Page Views); soft visual still healthy; re-click to restore if toggled off.
   - Soft **gear** icon open metric selector if present; list soft multi metrics; **Cancel/Escape without Save**; do not permanently deselect all series.
   - Soft hover crosshair/tooltip when series non-empty.
   - Soft Deselect All Metrics / multi-metric controls if present only with restore.
   - After lookback change: soft re-assert graph frame + legend or title still present.

7. Widget — Daily Averages (PDF + live bottom table)
   Purpose (PDF): daily averages for the metric selected in Details, broken down by page name and date.

   Live observations:
   - Title/body: **Daily Averages** + metric name (sample **Onload**); soft **Select metric** control text.
   - Table host id patterns: **table-for-average-performance-chart**, **table-for-average-performance-chart-table-viewport**, **table-for-average-performance-chart_pager_info**, **table-metric**.
   - Headers (sample when metric is Onload — unit may change with metric):
       **Page Name** | **YYYY-MM-DD (Sec)** × several recent dates | **30 Day Average**
     Soft unit variants: (Sec), (MS), %, unitless — when card selection changes, date column unit may change.
   - Pagination samples: “1 to 10 (25)”; page sizes **5 / 10 / 25 / 50 / 100 / 250 / 500 / All**; **<< Previous** / **Next >>**; **Export** with formats soft **CSV · TSV · JSON · Array**.
   - Row samples (illustrative GDC — do not hard-code): PDP, Category, Homepage, Search Results, Checkout-vt, Sitelet Landing, numeric page ids, …
   - Cells may show numeric daily values or **--** empty-day markers — soft accept both.

   Cover:
   - Verify Daily Averages region + table with **Page Name** header when data/shell ready.
   - Soft-assert ≥1 date column and/or **30 Day Average** when data-bearing period.
   - Soft-assert ≥1 data row when period has pages; otherwise controlled empty + annotate + optional broader lookback once (e.g. Last 7 days) then restore.
   - Soft Page Name non-empty on data rows; numeric or -- on metric cells.
   - Soft **column sort** by Page Name when sortable; soft first-row signature can change.
   - Soft **pagination**: change page size (10→25 or 5→10); soft Next/Previous when multi-page; restore capture when possible (prefer not leaving All on large sets).
   - Soft **Export** open/close; soft assert format labels when menu opens; do **not** hard-assert downloaded file contents.
   - Soft **Select metric** control if independent of Details cards — open/cancel; prefer card-driven metric changes (PDF primary path).
   - Soft after Details card click: Daily Averages header metric text may change to match selected card (PDF).

8. Time Period / Time Lookback
   Host: #time-lookback (+ Filters Time Period when open).
   Live presets observed in probe.lookbackOptions (assert multi-option set; casing flexible):
   Custom Date Selection; Last 2/7/14/30/90 days; Today so far; This week so far; This month so far;
   Last 15/30 minutes; Last 1/3/6/12/24 hours. (Probe list may omit “Last 1 hour” when already selected or show it in chrome.)
   Cover:
   - Open menu; soft multi options.
   - **Required apply set:** Last **1 hour** or Last **6 hours**, Last **24 hours**, Last **7 days**, Last **30 days** — widgets ready soft; restore original lookback.
   - Soft Custom Date open/cancel only.
   - After each applied change: soft re-assert RUM Performance Detail still selected; Bar Graph + Performance Graph + Daily Averages still present or controlled loading complete; GDC site still selected.
   - Soft time-range text on graphs can change without asserting exact timestamps.

8b. Top chrome icons (tooltips + round-trip)
   Soft hover/click Filters, Share, Manager; return to site/dashboard + RUM Performance Detail.

8c. Site quick-switch + lock
   Soft switch Demo eCommerce Global (or alternate available site); soft assert widgets still RUM Performance Detail home; soft lock if present; **always restore GDC Test Site 2 + RUM Performance Detail**.
   Soft re-check widget site subtitle eventually matches GDC or quick site after settle (annotate lag if subtitle lags selector).

8d. Auto Refresh apply
   Soft apply 5 Minutes then Off/restore; leave non-aggressive (not 2m for remainder of suite unless restored).

8e. + Dashboard → Manager eye
   Soft eye View switch; restore **RUM Performance Detail** exact.

9. Filters drawer
   Hosts:
   - #toggle-filters / #mobile-controls-filters-btn
   - Apply / Apply Filters soft
   - #cancel-filters / Cancel
   - Soft filter labels common to dashboard shell: Time Period, Timezone, Site, Data Type / Data Originated From, Page Name / Content Groups / Page Groups, Traffic Segment, Bot Traffic, Common Devices, Campaign / Source / Medium soft when present, Data Science / graph residue fields from widget editor soft (Performance Metric, Displayed Metric, Graph Type, Group Lines By… appear when widget editor open — **do not Save**)
   - My Filters / Shared Filters
   - Clear Cache / Clear Genius Cache — **do not click**
   - Save Filter / Save as New / Save Current — **do not click**
   Cover:
   - Open Filters; verify Time Period / Timezone / Site soft when present.
   - Soft apply one dimensional filter (e.g. Page Name, Traffic Segment, or Bot Traffic when options exist); soft healthy or empty widgets; Cancel or re-apply default — **no Save Filter**.
   - Soft My/Shared tabs.
   - After Apply, verify RUM Performance Detail still selected and four widget groups present or controlled loading complete.
   - Soft close Filters before leaving suite home.

10. Graphs, tables, exports and interactions (cross-widget)
    - Soft Highcharts signatures for Bar Graph (#multi-step-bar-graph) and Performance Graph (#perfGraph-page-timings-graph) independently.
    - PDF path: Details card → Bar Graph metric + Daily Averages metric co-update; soft verify linkage for ≥2 card choices then restore Onload.
    - Soft legend toggle on Performance Graph; soft gear cancel.
    - Soft Export open/close on Daily Averages.
    - Soft Escape recovers menus/modals.
    - Soft a11y: filter toggle focus; Help chrome soft.
    - Soft compare: page rows on Daily Averages independent of Bar Graph category set (Bar Graph may show All Pages aggregate while table lists per-page days).
    - Soft “All Pages Average” / “Deselect All Metrics” if available — restore visible metric series before end.

11. Soft switches to sibling preconfigured dashboards
    Preconfigured observed live (probe switcher lists):
    Site Overview | VitalPulse | Synthetic Site Health | Traffic Source and Medium | **RUM Performance Detail** | Synthetic Performance Detail | Native App Performance Detail | Native App Detailed Metrics - Android | Native App Detailed Metrics - iOS
    Cover:
    - Soft switch once to a non-home option (e.g. Traffic Source and Medium or Site Overview) for ≤1 cycle; annotate difference; **restore RUM Performance Detail + GDC Test Site 2**.
    - Soft once to **Synthetic Performance Detail** to prove exact-name discrimination (not RUM); **restore RUM Performance Detail**.
    - Soft Shared dashboard sample optional; no save.
    - Soft sibling can change quick site — always re-assert GDC.

12. Combination, recovery and non-functional
    - Capture → lookback change → soft Bar + Details + Perf Graph + Daily Averages → restore lookback → dash still RUM Performance Detail.
    - Soft: click LCP (or INP) card → soft Daily Averages metric rename → soft Export menu close → restore Onload card.
    - Soft page-size change → restore.
    - Soft legend toggle Onload → restore.
    - Filters Apply soft → restore.
    - Refresh keeps four widget groups.
    - Soft Manager / wizard cancel.
    - Soft sibling switch → restore home + site.
    - Recover overlays, clear sticky searches if any, restore lookback/auto-refresh when restorable without Save.
    - Performance: widgets settle within project soft bounded deadlines.
    - Viewport: desktop + ~1100px; Daily Averages may horizontal-scroll across date columns — OK.

13. Out-of-scope mutations (always)
    - Do not Save Filter / Save as New / Save Current / permanent shared filters.
    - Do not Clear Cache / Clear Genius Cache.
    - Do not permanently Save dashboard settings, Share/Unshare, Clone for others, Delete, overwrite Preconfigured layout.
    - Do not Add Widget permanently / Save widget wizard / leave “Editing All Widgets” applied.
    - Do not Create permanent + Dashboard on shared account.
    - Do not leave Auto Refresh or Carousel aggressive.
    - Do not hard-code metric totals, page-name catalogs, date column strings, or exact time-period / timezone strings.
    - Do not expand into full Monitoring Performance Detail (scatter, session lookup, waterfall Performance Breakdown), RUM PO, AW, or Synthetic Performance Detail regression.
    - Do not leave Performance Graph with all series deslected if product allows zero series — restore default visibility if possible.
    - Do not leave sticky table search that hides all Daily Averages rows for next suites — clear/restore.

14. Live reverse-engineering facts to preserve (GDC Test Site 2, Aug 2026 probe)
    - URL: portal …/index.php?r=site/dashboard
    - Browser title & #page-title: Dashboards
    - Menu: Dashboards (biz.dashboards)
    - #select2-switch-dashboard-container: **RUM Performance Detail**
    - #select2-quick-site-id-container: GDC Test Site 2
    - Chrome: #time-lookback (sample Last 1 hour), #refresh-dashboard, Auto Refresh (5m) with Off/2m/5m/10m/15m/30m/60m, Carousel (off), + Widget, + Dashboard
    - Four primary UI groups:
        1) **Bar Graph** — #multi-step-bar-graph; title metric e.g. Onload; time-range subtitle
        2) **Details** — all selected pages (avg); metric cards listed in §5b; host sample page-onload-card
        3) **Performance Graph** — #perfGraph-page-timings-graph; “Page Timings Over Time for All Matched Pages”; multi-series legend
        4) **Daily Averages** — table-for-average-performance-chart*; Page Name + date columns + 30 Day Average; Select metric; pagination + Export (CSV/TSV/JSON/Array)
    - PDF sync: card click updates bar + daily averages; graph gear + legend control series; auto-refresh 2m–60m; Filters Apply reloads; time selector top
    - Lookback options in probe.lookbackOptions
    - AutoRefresh options in probe.autoRefreshOptions
    - Tabs: my-filters-tab / shared-filters-tab when filters open
    - Global site Select2 may be hidden; quick site is recovery anchor
    - Subtitle site on graphs may lag or differ briefly from quick site after switch — re-assert site + soft refresh
    - Help article id: **360033246414**
    - Monitoring npm rum-pd is **not** this suite

15. Refer to the attached documentation (PDF)
    PDF “The RUM Performance Detail Dashboard – Blue Triangle Help Center”:
    - How to find: Menu → Dashboards → dropdown **RUM Performance Detail**; alternate gear → eye
    - Widgets: Bar Graph & Details (card click updates bar + Daily Averages); Performance Graph (Page Timings Over Time for All Matched Pages; gear + legend); Daily Averages (page × date)
    - Auto-refresh: 2 minutes … once per hour
    - Filters: top Filter → Apply reloads; Time Period selector top of dashboard
    Prefer live metric card names, live legend series, live Daily Averages headers (including dynamic date columns and unit), live Export formats, live host ids. Do not invent Monitoring-only widgets (scatter, waterfall, BTT session lookup) not present on this preconfigured board. Ignore PDF TOC “Synthetic” typo.

3️⃣ TASK — EXACTLY WHAT TO GENERATE
Generate a separate Playwright automation suite under regression_tests > US2 > dashboards > preconfigured > rum-performance-detail as specified above. Cover thorough regression for:
  - navigation & identity (**exact RUM Performance Detail** on site/dashboard — not Monitoring Performance Detail, not Synthetic Performance Detail)
  - Bar Graph shell, range subtitle soft, optional All Pages / series controls soft
  - Details metric cards (labels, formats, tooltip soft) + **card → bar + daily averages** linkage
  - Performance Graph (Page Timings Over Time title, multi-series legend toggle, gear cancel soft)
  - Daily Averages table (Page Name, date columns / 30 Day Average, rows, sort soft, pagination, Export soft)
  - fields, labels, filters, graphs, tabs (My/Shared Filters)
  - Time Lookback options and restore (incl. 1h or 6h / 24h / 7d / 30d set)
  - Filters drawer soft + no Save Filter / no Clear Cache
  - Dashboard chrome: switcher, auto refresh (2m–60m bounds soft), refresh, manager, soft create cancel-only
  - combination, recovery, empty/soft data paths, quick-site re-anchor after switch
  - explicit non-confusion with Monitoring RUM Performance Detail, Synthetic Performance Detail, Traffic Source and Medium, Site Overview

Follow existing project framework and POM conventions. Reuse preconfigured dashboard chrome helpers. Prefer exact dashboard option text **RUM Performance Detail**.

After generation:
1. Execute only the newly created RUM Performance Detail **Dashboard** regression spec first and fix automation hard failures.
2. Generate fresh isolated Allure results and an HTML report for this spec.
3. Generate a manual test-case workbook (Summary / Regression TCs / Notes).
4. Include executed result summary and soft annotations for: empty Daily Averages, graph site subtitle lag, Test Override metric presence, Export menu variants, legend/series restore, Monitoring suite non-overlap, sibling Synthetic name discrimination.

4️⃣ OUTPUT FORMAT
- .ts for Playwright automation / POM / locators
- .html for the fresh Allure report
- .xlsx for manual test cases

5️⃣ CONSTRAINTS — RULES YOU MUST FOLLOW
1. Do NOT generate API or Database test cases.
2. Use existing framework methods whenever possible instead of creating duplicate methods.
3. Strictly follow the POM structure defined in the framework.
4. Use standard TypeScript and Playwright coding practices.
5. Use stable control IDs and widget **titles/headers** (#multi-step-bar-graph, #perfGraph-page-timings-graph, table-for-average-performance-chart*, Details/Bar Graph/Daily Averages text); do not depend on dynamic Highcharts instance IDs alone.
6. Do not hard-code onload/LCP/INP/CLS/page-view totals, page-name sets, fixed time-period / timezone strings, or exact “1 to N (M)” pager strings.
7. Capture and restore RUM Performance Detail selection, GDC Test Site 2, Time Lookback and Auto Refresh where possible; restore Details selected metric to a sane default (Onload) when tests change cards.
8. Use a serial suite only when shared page state is intentional; optional/data-dependent failures must not abort unrelated tests (soft deadlines + recovery).
9. Use bounded soft deadlines and explicit action timeouts.
10. Do not use Promise.race soft deadlines in a way that leaves losing page actions running into later serial tests; cancel/reload/reset before continuing.
11. Do not use expect.soft inside a soft try/catch when it would still mark a serial test failed; use hard expect inside try/catch and annotate the fallback.
12. Do not Save Filter, Clear Cache, permanently Save/Clone/Share/Delete dashboards, or permanently Add Widget on this shared board; do not leave Editing All Widgets applied.
13. Keep primary scope on site/dashboard + RUM Performance Detail; restore that home after soft sibling visits. Never target Monitoring → Real User Browser → Performance Detail as suite home.
14. Close all menus, overlays, drawers, modals, Dashboard Manager, widget wizard, Export menus, gear menus, pager menus opened by tests.
15. Do not claim widgets refreshed by merely counting SVG hosts; compare scoped widget signatures, titles, Daily Averages metric header, or lookback text.
16. Treat timing units as format-tolerant (s/S/ms/MS) and page-view K/M suffixes as format-tolerant; CLS unitless soft.
17. Never accept **Synthetic Performance Detail**, **Native App Performance Detail**, or partial “Performance Detail” Shared options as suite home — exact **RUM Performance Detail** only. Do not bind suite npm to test:regression:us2:rum-pd (Monitoring).
18. If any requirement is ambiguous or missing, flag it before generating automation.

6️⃣ AMBIGUITIES TO FLAG BEFORE AUTOMATION
- Package had **PDF only** (no companion product .txt). This AI Prompt + probe JSON are the automation truth layer.
- PDF TOC mislabels navigation section as Synthetic — ignore; select live **RUM Performance Detail**.
- Graph/site subtitle may show Demo eCommerce (or other) while quick site is GDC after first paint — always re-assert GDC; soft annotate subtitle lag; soft refresh may be needed for subtitle alignment.
- Details shows many metric cards; which are “default visible” vs scrolled may be build-dependent — assert core CWV + Onload + Page Views + First Byte subset hardish; others soft.
- **TEST OVERRIDE** appears on GDC under some states and may appear in Performance Graph legend — soft extra, not a hard product requirement on all accounts.
- Daily Averages date column set and unit suffix ((Sec)/(MS)/…) follow selected metric and calendar — soft presence of Page Name + ≥1 measure column; do not hard-code calendar dates.
- “Select metric” control vs Details-card-driven metric selection: PDF attributes Daily Averages metric to Details cards; if both exist, prefer cards as primary path; soft both without sticky unexpected metric at suite end.
- Export formats CSV/TSV/JSON/Array observed — build may add/remove; soft open/close only.
- Page-size “All” on large page lists may be slow — soft prefer 10/25; do not leave All if suite timing suffers.
- Bar Graph “All Pages” / multi-page bars vs Daily Averages multi-page rows may not share identical page set — soft compare structures only.
- Gear menu on Performance Graph may share chrome with widget editor — Escape/Cancel only; never Apply permanent widget layout edits.
- Soft sibling preconfigured may change quick site — always re-anchor GDC Test Site 2.
- Global #select2-site-id-container often hidden — quick site is recovery anchor.
- Formal SLA for widget reload after lookback/card click — use project soft deadlines.
- Coach/Get started overlays may appear.
- Monitoring Performance Detail help (42336918655763) documents features **not** on this dashboard (scatter, session lookup, waterfall) — do not fail this suite if those are absent.
- Relationship claim “view of RUM Performance Detail page” (PDF) does not require deep equality of filters/widgets with Monitoring page.
- Portal may rename legend short forms (1stByte vs First Byte) — soft dual regex.

7️⃣ EXAMPLE — MATCH THIS STYLE & DEPTH
Refer to the existing POM framework and regression suites, especially:
- tests/regression_tests/US2/dashboards/preconfigured/site-overview/site.overview.dashboard.regression.spec.ts
- tests/regression_tests/US2/dashboards/preconfigured/vital-pulse/vital.pulse.dashboard.regression.spec.ts
- tests/regression_tests/US2/dashboards/preconfigured/synthetic-site-health/synthetic.site.health.dashboard.regression.spec.ts
- tests/regression_tests/US2/dashboards/preconfigured/traffic-source-and-medium/traffic.source.medium.dashboard.regression.spec.ts
- pages/SiteOverviewDashboardPage.ts / pages/VitalPulseDashboardPage.ts / pages/SyntheticSiteHealthDashboardPage.ts / pages/TrafficSourceMediumDashboardPage.ts
- helpers/preconfiguredDashboardChrome.ts
- pages/MarketingOverviewPage.ts / locators/MarketingOverviewLocators.ts
- competitive.index.table / traffic-source-and-medium (sort/pagination/export soft patterns)

Follow their patterns for:
- POM/locator separation
- authenticated profile/site selection (GDC Test Site 2) with quick-site recovery
- Select2 dashboard switch + restore (**exact RUM Performance Detail**)
- time lookback / auto refresh menus
- filter open/apply/cancel + no Save Filter
- Highcharts shell + legend soft toggles
- table sort/pagination/export soft without hard file asserts
- serial recovery after soft sibling switches
- isolated Allure + Excel workbooks
- explicit npm naming that does **not** collide with Monitoring test:regression:us2:rum-pd
