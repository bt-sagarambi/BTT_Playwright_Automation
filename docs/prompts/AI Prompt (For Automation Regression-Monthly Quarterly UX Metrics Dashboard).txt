1️⃣ PERSONA
You are a Senior QA Engineer with 15+ years of experience in web application testing. You specialize in functional, UI, data validation, performance, accessibility and non-functional testing. You have hands-on experience in QA automation using Playwright with TypeScript in VS Code and Cursor IDE, including MCP integration. You are an expert in designing and maintaining automation test cases using the Page Object Model (POM) in a QA automation framework. You can also generate manual test cases for the automation coverage you implement.

2️⃣ CONTEXT
You are generating thorough regression test cases for the **“Monthly/Quarterly - UX Metrics”** Business Intelligence dashboard. Reverse-engineer the live application using **GDC Test Site 2** and refer to:
  • Attached PDF / Help package: **“BI Dashboard: Period-over-Period Key Metrics Dashboards”**
      docs/prompts/BI_Dashboard_Period_over_Period_Key_Metrics_Dashboards.pdf
      (Help article: https://help.bluetriangle.com/hc/en-us/articles/37570512713875-BI-Dashboard-Period-over-Period-Key-Metrics-Dashboards)
  • PDF / Help extract notes: docs/prompts/monthly-quarterly-ux-metrics-pdf-extract.txt
  • Shared BI overview (soft sibling doc only): **“Blue Triangle - Business Intelligence”**
      https://help.bluetriangle.com/hc/en-us/articles/32871857479571-Blue-Triangle-Business-Intelligence
  • Live portal reverse-engineering probe (Sep 2026): docs/prompts/monthly-quarterly-ux-metrics-live-probe.json
  • Live notes / screenshots: docs/prompts/monthly-quarterly-ux-metrics-live-notes.txt ,
      docs/prompts/monthly-quarterly-ux-metrics-live.png , docs/prompts/monthly-quarterly-ux-metrics-dashboard.png
  • Probe script: scripts/probe-monthly-quarterly-ux-metrics.ts
  • Soft sibling BI catalog / PoP family for shared chrome patterns only (do not treat as home):
      Monthly/Quarterly Revenue, Daily/Weekly UX Metrics & Revenue variants,
      CWV Top 10 Period over Period (PoP), CWV Trends, Site Operations Dashboard for Page Names,
      Site Operations + CWV Trends
  • Soft sibling portal shells (different product surfaces — soft hop only):
      docs/prompts/AI Prompt (For Automation Regression-Core Web Vitals).txt
      docs/prompts/AI Prompt (For Automation Regression-Vital Pulse Dashboard).txt
      docs/prompts/AI Prompt (For Automation Regression-CWV Top 10 Period over Period PoP).txt

NOTE ON DOCUMENTATION VS LIVE PRODUCT
1) **Help/PDF attached** is SoT for the **Period-over-Period Key Metrics** product family, the **UX Metrics** metric inventory (**Onload, LCP, CLS, INP**), comparative visualization purpose, and **Filter Options** inventory/defaults below. Prefer **live** `#bi-iframe` control hosts / Comparison Period / End Date chips / Save As / Refresh Data / Reset to Defaults / Switch metric / Export chart labels when PDF is silent or older than the BI shell.
2) Live portal path (probe Sep 2026): Menu → **Advanced Reporting & Alerting** → **Custom Reporting** → **Business Intelligence** → (BI iframe) **Dashboards** → Search **“Monthly/Quarterly - UX Metrics”** → open the matching card/result.
3) Portal shell route: **business-intelligence/tool** (sid query soft). Browser / #page-title soft: **Business Intelligence**.
4) Product UI host is the embedded BI app iframe **`#bi-iframe`** → `https://jbi.bluetriangletech.com/…` (JWT token query — do not hard-code tokens). **All dashboard widgets / Filters / Save As / Refresh Data / Reset to Defaults / per-widget gear & hamburger live inside this iframe** (or nested frames). Portal chrome (site Select2, Get started, portal Filters funnel) is outer shell only — **do not confuse** portal `#toggle-filters` with BI **Filters** on the dashboard viewer.
5) Live catalog card (GDC): title **Monthly/Quarterly - UX Metrics**; type badge **Grid**; soft ~**25 widgets**; sibling **Monthly/Quarterly Revenue** (~31 widgets) — never open Revenue as suite home.
6) Help/PDF Introduction objective: comprehensive overview of key metrics in a **comparative period-over-period** manner for predefined time buckets (this suite = **Monthly + Quarterly** UX Metrics combined dashboard).
7) Help/PDF UX Metrics inventory (THIS dashboard): **Onload (Seconds), LCP (Seconds), CLS (Score), INP (Seconds)** — each with timeframe, KPI, and comparative trendline.
8) Live viewer soft identity: title **Monthly/Quarterly - UX Metrics**; folder badge **Global**; layout **Responsive Grid**; toolbar **Refresh Data | Save As | Reset to Defaults | Filters**.

CRITICAL DISTINCTION (do not confuse screens)
  • **Monthly/Quarterly - UX Metrics (THIS suite)** = BI iframe Dashboards → exact title match **/Monthly\/Quarterly\s*-\s*UX Metrics/i**; portal route **business-intelligence/tool**.
  • **Monthly/Quarterly Revenue** — BI catalog sibling; soft presence only; restore UX Metrics.
  • **Daily/Weekly UX Metrics or Revenue** — same PoP family; different catalog cards; soft only.
  • **CWV Top 10 PoP / CWV Trends / Site Operations Page Names** — BI siblings; soft only.
  • **VitalPulse** (preconfigured) / **Core Web Vitals (VitalScope)** (RUM Performance Overview) — **not** suite home.
  • Portal **Dashboards** (Site Overview etc.) — unrelated shell; out of primary scope.
  • BI **AI Query / Charts / Schedules / Folders / Favorites** tabs — soft chrome only; always restore **Dashboards → Monthly/Quarterly - UX Metrics**.

⚠️ PROBE / MUTATION PITFALLS
1) **iframe SoT:** Never assert widget text from the portal `body` alone. Scope locators to `#bi-iframe` contentFrame (and nested frames if present). Re-acquire frame after navigation / Search / open dashboard.
2) **Search** on Dashboards list: use the visible **Search…** input; open the **title link** of **Monthly/Quarterly - UX Metrics** (not Monthly/Quarterly Revenue / Grid badge / heart / ⋮ alone). Confirm viewer shows that exact title before widget asserts.
3) **Comparison Period** + **End Date** + dimensional filters mutate shared dashboard filter state — capture original filter signature at suite start; **restore** (or **Reset to Defaults**) before suite end.
4) **Save As** clones a dashboard — use a **disposable clone name** with unique suffix (timestamp/random) e.g. `MQ-UXM-QA-<unique>`; verify clone appears; then **delete/clean up** the clone if UI allows, or annotate leftover clone for manual cleanup. Never overwrite shared Global templates permanently.
5) **Refresh Data** — soft click and assert widgets re-settle (signature / loading→ready); not destructive.
6) **Reset to Defaults** (live plural) — soft verify it restores default filter/comparison/end-date state after intentional changes; if restricted on locked Global dashboard, annotate and use Save As clone for mutation tests.
7) Do **not** permanently edit shared Global dashboard widgets/layout, create production Schedules, or leave AI Query / Chart Builder drafts saved.
8) Do **not** Save portal Genius Filters / Clear Genius Cache from outer shell.
9) KPI values, delta %, date-axis tick labels, period-range strings (e.g. `Jul 03, 2026 - Aug 02, 2026 vs …`), widget instance ids, JWT tokens, and “Last modified …” strings are **data-dependent** — never hard-code probe samples; assert formats, labels, Current/Previous legend language, and before/after soft signatures.
10) After filter changes / Refresh / Reset / Save As / Switch metric / export / sibling hop, always re-assert **GDC Test Site 2** + **Monthly/Quarterly - UX Metrics** identity.
11) Highcharts / chart SVG ids inside BI are dynamic — prefer widget titles (**Onload Monthly**, **LCP Quarterly**, …), series labels (**Current Onload**, **Previous LCP**, …), and KPI card titles.
12) If Search yields empty catalog or open fails, assert controlled empty, annotate entitlement/folder scope, try folder filter **All Folders** / soft Global folder, and fail soft only after recovery attempts.
13) **Switch metric** (gear icon, live `title="Switch metric"`) changes the active metric for that widget — capture before/after soft title or KPI label; restore original metric when practical.
14) **Export chart** hamburger is **per-widget** (probe: 16). Dashboard-level export (user-required top-right hamburger → PNG/PDF/PowerPoint) may be separate — locate in viewer chrome during automation; soft cross-check exported labels vs UI without pixel OCR golden files.

Live observations (GDC Test Site 2 — probe Sep 2026)
- Portal URL: …/index.php?r=business-intelligence/tool&sid=…
- Portal title / #page-title: **Business Intelligence**
- Site: GDC Test Site 2
- Outer iframe: `#bi-iframe` → jbi.bluetriangletech.com
- BI left rail soft: **Favorites | Dashboards | AI Query | Charts | Schedules | Folders** (+ **AI Help**)
- Dashboards list chrome soft: **+ Create Dashboard**; pager **5/10/25/50 / Page**; **Search…**;
    folder filter (**All Folders / My Folder / Global / Site Folder / Account Folder / Development**);
    status filter; sort (**Last Modified Desc** etc.); **Local | UTC**; **Reset**; grid/list toggles; favorites heart
- Catalog card soft for THIS dashboard:
    **Monthly/Quarterly - UX Metrics** · Grid · ~**25 widgets**
- Soft sibling cards nearby: Monthly/Quarterly Revenue, CWV Top 10 PoP, Site Operations Page Names, CWV Trends
- In-dashboard viewer chrome (REQUIRED — live hosts):
    **Back** | title **Monthly/Quarterly - UX Metrics** | **Global** | **Responsive Grid**
    **Refresh Data** | **Save As** | **Reset to Defaults** | **Filters**
- Live Filters panel (REQUIRED — user regression point #1):
    **Comparison Period** — Default (from chart) | Directly Previous Period | Same Time Last Year
      (maps Help/PDF **Comparison Method**: vs previous / vs same period last year)
    **End Date** — Today | Yesterday | Start of Week (Mon/Sun) | Start of Month |
      Start of Previous Month | Start of Quarter | Custom
    **Browser** | **Bot Traffic** (default exclude bots — soft) | **Device** (Desktop/Mobile)
    **Operating System** | **Originated From** (RUM Browser / Native App / Webview)
    **Page Name** | **Page Group** | **Return/New Visitor** | **Traffic Segment**
    **Percentile** — p5…p99 incl **p75** (Help default 75th)
- Live widget families (hard inventory soft-assert):
    **Monthly trend charts:** Onload Monthly, LCP Monthly, CLS Monthly, INP Monthly
      (each with **Current** / **Previous** series + date axis)
    **Quarterly trend charts:** Onload Quarterly, LCP Quarterly, CLS Quarterly, INP Quarterly
    **KPI number cards:** Dynamic Chart Metrics Monthly Number Charts;
      Dynamic Chart Metrics Quarterly Number Charts
      (metric long names + **Prev:** + ▲/▼ delta % — format-tolerant)
    Soft period subtitle examples:
      Monthly: `Jul 03, 2026 - Aug 02, 2026 vs Aug 02, 2026 - Sep 01, 2026`
      Quarterly: `Mar 03, 2026 - Jun 02, 2026 vs Jun 02, 2026 - Sep 01, 2026`
- Per-widget chrome (REQUIRED — user regression points #3 & #5):
    Gear **`Switch metric`** — 16 instances on probe (user: Active metric)
    Hamburger **`Export chart`** — 16 instances on probe
- User-required flows (live + user spec):
    1) Filter combinations → data refresh implications (Comparison Period, End Date, Browser, OS, Return/New Visitor, Percentile, …)
    2) **Save As** clone; **Refresh Data**; **Reset to Defaults**
    3) Dashboard-level top-right **hamburger** → **PNG Image / PDF Document / PowerPoint** + soft UI↔document cross-check
    5) Per-widget **gear → Switch metric** + verify impact; per-widget **hamburger → PNG / PDF / CSV Data** + soft cross-check

1. Business purpose, site and data handling
   - Site: GDC Test Site 2
   - Data center: US
   - Monthly/Quarterly - UX Metrics presents **period-over-period comparative UX performance** (Onload, LCP, CLS, INP) across **monthly and quarterly** visualizations plus KPI summary cards.
   - Metric formats soft: seconds for Onload/LCP/INP, unitless score for CLS, % deltas on KPI cards, date-range comparison strings — format-tolerant; **never hard-code** numeric KPI values or axis tick dates from the probe.
   - Use live UI data only. Do not call APIs or query a database.
   - Capture at suite start (inside BI iframe after dashboard open): dashboard title, Comparison Period value, End Date value, key filter chip/values, presence of monthly+quarterly widget titles, Current/Previous legend presence, Switch metric / Export chart control counts soft, overlays closed, clone-name none.
   - Assert labels, formats, chart/table shells, KPI card Prev/delta language, controlled empty states, before/after soft signatures rather than exact backend series.
   - If a filter combo yields empty charts/KPI cards, assert controlled empty, annotate, Reset to Defaults / restore captured filters and continue.
   - Do not hard-code JWT tokens, nested chart instance ids, or Highcharts internal ids.

2. Navigation and implementation paths
   Live primary path (user + probe):
   Login → Left menu **Advanced Reporting & Alerting** → **Custom Reporting** → **Business Intelligence**
   → portal **business-intelligence/tool** → wait `#bi-iframe` ready → BI rail **Dashboards**
   → Search field **“Monthly/Quarterly - UX Metrics”** (or shorter unique token **“UX Metrics”** with guard against Revenue)
   → click the **displayed result / card title** → dashboard viewer.

   Soft alternate: BI Favorites if already favorited; Folders → Global → same title; Help deep-link if present — still require exact UX Metrics title (not Revenue).

   Target automation folder:
   tests/regression_tests/US2/advanced-reporting/custom-reporting/business-intelligence/monthly-quarterly-ux-metrics/
   Suggested spec name:
   monthly.quarterly.ux.metrics.regression.spec.ts
   Suggested POM / locators:
   pages/MonthlyQuarterlyUxMetricsDashboardPage.ts
   locators/MonthlyQuarterlyUxMetricsDashboardLocators.ts
   Reuse patterns: portal session + LeftNav expand Advanced/Custom Reporting; iframe frame helpers from CWV Top 10 PoP / Site Ops Page Names suites;
   soft Search/Reset on BI Dashboards list; recover via re-goto business-intelligence/tool + re-open dashboard.
   Do **not** treat VitalPulse / Site Overview / Core Web Vitals / Monthly Quarterly Revenue POM as home.
   Suggested manual workbook:
   docs/Monthly_Quarterly_UX_Metrics_Regression.xlsx
   Suggested npm command:
   test:regression:us2:monthly-quarterly-ux-metrics
   Suggested test case ID prefix:
   REG-MQ-UXM-###
   Probe:
   scripts/probe-monthly-quarterly-ux-metrics.ts → docs/prompts/monthly-quarterly-ux-metrics-live-probe.json

3. Page load, authentication and default context
   - Authenticated navigation without login redirect, blank BI shell, or perpetual `#jbi-iframe-loading`.
   - Portal browser title / #page-title matches /Business Intelligence/i.
   - URL contains business-intelligence/tool (slash / %2F safe).
   - Selected site is GDC Test Site 2.
   - Dismiss portal Get started / Don’t show me again / jconfirm before iframe work.
   - Wait for `#bi-iframe` attached + contentFrame body text length soft-ready.
   - Open Dashboards → Search → open **Monthly/Quarterly - UX Metrics**.
   - Verify BI viewer identity: title matches /Monthly\/Quarterly\s*-\s*UX Metrics/i.
   - Verify core inventory settles within a bounded UI deadline (Help/PDF + live titles):
       • Soft-assert **monthly** chart sections: Onload / LCP / CLS / INP Monthly
       • Soft-assert **quarterly** chart sections: Onload / LCP / CLS / INP Quarterly
       • Soft-assert **Dynamic Chart Metrics** Monthly + Quarterly number-chart regions
       • Soft-assert **Current** / **Previous** series labels on ≥1 trend chart
       • Soft-assert Filters chrome reachable (**Comparison Period**, **End Date**, Browser, OS, Return/New Visitor, Percentile, …)
       • Soft-assert toolbar **Refresh Data / Save As / Reset to Defaults**
       • Soft-assert ≥1 **Switch metric** gear and ≥1 **Export chart** hamburger on widgets
   - Soft-assert catalog ~25 widgets vs live visible families — annotate extras/mirrors; prefer PDF four UX metrics × monthly/quarterly views as primary inventory.
   - Back/Forward/refresh: restore BI tool + re-open UX Metrics if iframe resets; re-select GDC if lost.
   - Capture uncaught page errors; distinguish portal noise vs BI iframe errors.

4. BI shell chrome (Favorites / Dashboards list / Search) — REQUIRED before viewer
   Cover:
   - Soft-assert BI rail items Favorites / Dashboards / AI Query / Charts / Schedules / Folders.
   - Soft Favorites empty/controlled state OK; not suite home.
   - On Dashboards: Search for UX Metrics token → ≥1 matching card; **exclude** Monthly/Quarterly Revenue false-positive; clear search before suite end if sticky.
   - Soft folder/status/sort/Local-UTC/Reset on list toolbar — non-destructive; restore All Folders + default sort when changed.
   - Soft grid vs list toggle then restore.
   - Soft ⋮ / heart on card — open/close only; do not leave unwanted favorite sticky if avoidable.
   - Soft Create Dashboard / New Schedule — open/cancel only; **no permanent create**.
   - Soft sibling card presence (Monthly/Quarterly Revenue / CWV Top 10 PoP / Site Ops Page Names) once; do not open deep; stay on list or restore UX Metrics viewer.

5. Dashboard viewer identity + layout (Help/PDF Dashboard Components + live widget map)
   Cover:
   - Soft-assert dashboard title **Monthly/Quarterly - UX Metrics**.
   - Soft **Back** to Dashboards then re-open (recovery pattern).
   - Soft-assert **Global** folder badge and **Responsive Grid** layout label when present.
   - Soft-assert Help/PDF **UX Metrics** comparative visualizations for **Onload, LCP, CLS, INP** across monthly AND quarterly sections.
   - Soft-assert each trend chart shows comparative behavior: **Current** vs **Previous** series (or equivalent legend).
   - Soft-assert KPI number cards show metric long name, current value, **Prev:** prior value, and ▲/▼ delta annotation (format-tolerant).
   - Soft-assert period comparison subtitle strings appear on charts/cards (monthly vs quarterly ranges differ — do not hard-code probe dates as expected constants).
   - Soft scroll all major widget regions into view; assert no perpetual spinner per widget (bounded).
   - Soft-assert duplicate mirrored chart pairs (probe shows 2× each monthly/quarterly block) — annotate; do not hard-fail on duplicate layout alone.

6. Comparison Period + End Date filters — REQUIRED (user regression point #1; Help/PDF Filter Options)
   Cover:
   - Open **Filters** panel; capture baseline **Comparison Period** + **End Date** signatures.
   - **Comparison Period** samples (restore after each):
       • **Directly Previous Period** (maps Help: vs previous)
       • **Same Time Last Year** (maps Help: vs same period last year)
       • Restore **Default (from chart)** or baseline
     Soft-assert dashboard data refreshes: loading clears; KPI/chart soft signature or period subtitle changes **or** controlled identical annotate if data sparse.
   - **End Date** samples (restore after each; prefer non-destructive presets):
       • **Yesterday** or **Start of Previous Month** or **Start of Quarter**
       • Soft **Custom** date only if UI allows disposable pick without permanent schedule side-effects
     Soft-assert axis labels / period subtitle / KPI values shift or controlled empty + annotate.
   - Live probe: **no explicit Apply button** (`hasApply: false`) — filters may auto-apply on selection; still assert refresh after change; annotate if Apply appears on other builds.
   - **Restore** End Date + Comparison Period to baseline (or Reset to Defaults) before unrelated tests.

7. Dimensional filters + combinations — REQUIRED (user regression point #1; Help/PDF + live inventory)
   Cover soft combinations (sample ≥3–4 non-destructive combos; restore after each):
   - **Browser** — sample one browser value when data exists; soft-assert chart/KPI signature change or empty + annotate
   - **Operating System** — sample one OS value
   - **Return/New Visitor** — sample Returning vs New (or equivalent live values)
   - **Percentile** — soft default **p75** when readable; sample alternate (e.g. p50 or p90) and assert CWV-related widgets react (Onload/LCP/CLS/INP) while format-tolerant
   - Additional soft samples when time allows: **Bot Traffic** (default Non-Bot/exclude bots), **Device** (Mobile/Desktop), **Originated From**, **Page Name**, **Traffic Segment**, **Page Group**
   - For each change: assert widgets refresh (before/after signature on ≥1 monthly chart + ≥1 quarterly chart + ≥1 KPI card) or controlled empty + annotate
   - Cross-check at least one combo that keeps widgets non-empty when data exists on GDC Test Site 2
   - Always restore baseline filters before Save As / Switch metric / export tests
   - Soft: do **not** create permanent scheduled reports; Schedules UI open/cancel only

8. Save As / Refresh Data / Reset to Defaults — REQUIRED (user regression point #2)
   A) **Save As** (clone)
      - Soft-assert **Save As** control present in viewer toolbar.
      - Open Save As; enter disposable name `MQ-UXM-QA-<unique>`; confirm save.
      - Soft-assert clone opens or appears in Dashboards list Search.
      - Soft-assert clone shows same widget family titles (monthly/quarterly Onload/LCP/CLS/INP — not empty shell).
      - Cleanup: delete clone via ⋮/Folders if available; else annotate leftover name.
      - Never Save As overwriting the shared Global **Monthly/Quarterly - UX Metrics** title.

   B) **Refresh Data**
      - Capture soft widget / KPI signature.
      - Click **Refresh Data**.
      - Assert loading→ready; widgets still present; signature may match or change — annotate.
      - Identity still UX Metrics + GDC.

   C) **Reset to Defaults**
      - Intentionally change Comparison Period, End Date, and one dimensional filter.
      - Click **Reset to Defaults** (also accept Reset to Default).
      - Soft-assert filters return toward defaults / captured baseline; widgets refresh.
      - If control missing on read-only Global dashboard: annotate; perform Reset checks on the Save As clone instead.

9. Dashboard-level hamburger export — PNG / PDF / PowerPoint — REQUIRED (user regression point #3)
   Cover:
   - Locate **top-right dashboard hamburger / export** control in viewer chrome (distinct from per-widget Export chart when both exist).
   - Open menu; soft-assert **PNG Image**, **PDF Document**, **PowerPoint** (or PPT). If PowerPoint absent annotate live menu and still exercise PNG + PDF.
   - For each available option:
       • Capture UI soft signature first (dashboard title, Onload/LCP/CLS/INP presence, Current/Previous labels, one KPI card title, one period subtitle token).
       • Trigger export; handle download / new tab / preview with bounded timeout.
       • Soft cross-verify exported artifact against UI:
            - PNG: download/preview shows readable title or metric labels when extractable; no pixel golden.
            - PDF: soft assert text layer (when extractable) contains dashboard identity and/or UX metric labels matching UI; format-tolerant.
            - PowerPoint: same soft label match if option exists.
       • Close preview / dismiss dialogs; do not leave downloads blocking later tests.
   - Identity still UX Metrics + GDC after exports.

10. Per-widget gear (Switch metric / Active metric) — REQUIRED (user regression point #5)
   Cover:
   - Identify visible widget panels with gear icon (live `title="Switch metric"` — user may call **Active metric**).
   - Sample ≥2 widgets across different families (e.g. one **Onload Monthly** chart + one **Dynamic Chart Metrics Monthly Number Chart**):
       • Capture soft metric title / KPI label before switch.
       • Click gear → open metric picker menu.
       • Select an alternate metric if offered (e.g. switch among Onload / LCP / CLS / INP or related UX metrics).
       • Soft-assert widget title, axis label, or KPI long name updates to reflect new metric **or** annotate if menu is read-only/info-only on Global dashboard.
       • Restore original metric when practical (re-select or Reset to Defaults).
   - Do not leave widgets on unexpected metrics for later serial tests.

11. Per-widget hamburger export — PNG / PDF / CSV Data — REQUIRED (user regression point #5)
   Cover:
   - On the same sampled widgets (§10), locate **`Export chart`** hamburger (`title="Export chart"`).
   - Open menu; soft-assert **PNG Image**, **PDF Document**, **CSV Data** (PowerPoint may be absent at widget level — annotate).
   - For each available option on ≥1 widget:
       • Capture widget-level UI signature (widget title, Current/Previous legend, visible axis or KPI value format).
       • Trigger export; soft cross-check downloaded/opened artifact labels vs UI (same rules as §9 — label-level, not pixel OCR golden).
       • Close menus / revoke object URLs.
   - Repeat soft sample on one **quarterly** widget if menu options differ from monthly.
   - Identity still UX Metrics + GDC after exports.

12. Graphs / tables / tabs / a11y
   - Soft assert independent chart hosts for monthly AND quarterly UX metric families (titles — not SVG counts alone).
   - Soft hover one **Current** and one **Previous** series for tooltip presence (format-tolerant); no hard series values.
   - Soft-assert BI rail tabs (Favorites/Dashboards/AI Query/Charts/Schedules/Folders) reachable from viewer via Back + rail — always return to UX Metrics viewer.
   - Soft Escape closes Filters panel, Switch metric menus, export menus, Save As dialog.
   - Soft keyboard focus on Filters, Save As, Refresh, Comparison Period, End Date, one gear, one Export chart.

13. Sibling discrimination (soft)
   - Soft once **Monthly/Quarterly Revenue** or CWV Top 10 PoP from Dashboards list → restore **Monthly/Quarterly - UX Metrics** viewer + GDC.
   - Soft portal left-nav Core Web Vitals / VitalPulse is not suite home — if hopped, return to business-intelligence/tool + UX Metrics.
   - Always re-assert GDC Test Site 2 after site-like switches.

14. Combination, recovery, non-functional
   - Capture → Comparison Period change → End Date change → Browser+Percentile combo → Refresh Data → Reset/restore → dashboard export soft → widget Switch metric sample → widget export soft → Save As clone soft cleanup.
   - Refresh portal page: re-enter iframe → Dashboards Search → UX Metrics.
   - Performance: iframe + widgets settle within project soft deadlines.
   - Viewport: desktop + ~1100px OK (BI sidebar may collapse; Responsive Grid reflow soft).

15. Out-of-scope mutations (always)
   - Do not permanently modify Global UX Metrics layout/widgets.
   - Do not leave unrestored Comparison Period / End Date / dimensional filter changes on the shared dashboard.
   - Do not create permanent Schedules / AI Query saved assets / Chart Builder publishes.
   - Do not Clear Genius Cache / Save portal Filters.
   - Do not expand into full Monthly/Quarterly Revenue / CWV Top 10 PoP / VitalPulse suites as primary scope.
   - Do not hard-code KPI catalogs or date-axis tick lists from the probe.

16. Live reverse-engineering + Help/PDF facts to preserve
   - Portal route business-intelligence/tool; title Business Intelligence
   - `#bi-iframe` → jbi.bluetriangletech.com
   - BI Dashboards Search finds **Monthly/Quarterly - UX Metrics** (Grid, ~25 widgets)
   - Soft list chrome: Search, All Folders, Local/UTC, Reset, Create Dashboard
   - Help/PDF PoP family: UX Metrics = Onload, LCP, CLS, INP; comparative KPI + trendline per metric
   - Live filters: Comparison Period, End Date, Browser, Bot Traffic, Device, OS, Originated From,
     Page Name, Page Group, Return/New Visitor, Traffic Segment, Percentile (p75 default)
   - Live widgets: monthly + quarterly trend charts; Dynamic Chart Metrics number cards; Current/Previous series
   - Per-widget: Switch metric (gear) ×16; Export chart (hamburger) ×16
   - Required user flows: filter combos + data impact; Save As/Refresh/Reset; dashboard PNG/PDF/PPT export cross-check;
     per-widget Switch metric + PNG/PDF/CSV export cross-check
   - Probe JSON keys soft: shellUrl, wentDashboards, opened, dashboard, chromeHints, exportProbeDashboard, exportProbeWidget, gearProbe, screenshots, savedAt
   - Help URL: https://help.bluetriangle.com/hc/en-us/articles/37570512713875-BI-Dashboard-Period-over-Period-Key-Metrics-Dashboards
   - PDF path (when attached): docs/prompts/BI_Dashboard_Period_over_Period_Key_Metrics_Dashboards.pdf

3️⃣ TASK — EXACTLY WHAT TO GENERATE
Generate a Playwright automation suite under
tests/regression_tests/US2/advanced-reporting/custom-reporting/business-intelligence/monthly-quarterly-ux-metrics
covering thorough regression for:
  - navigation & identity (portal BI tool + iframe Dashboards Search → exact Monthly/Quarterly - UX Metrics)
  - BI rail / Dashboards list chrome (Search, folders, soft siblings incl. Monthly/Quarterly Revenue)
  - dashboard viewer widgets: monthly + quarterly Onload/LCP/CLS/INP charts + Dynamic Chart Metrics KPI cards + Current/Previous legends
  - Comparison Period + End Date changes + dimensional filter combinations + refresh/empty handling + restore
  - Save As clone (disposable) + Refresh Data + Reset to Defaults
  - dashboard-level hamburger export PNG/PDF/PowerPoint with soft UI↔document cross-check
  - per-widget Switch metric (gear) impact + per-widget Export chart PNG/PDF/CSV with soft cross-check
  - recovery / GDC re-anchor / non-confusion with Revenue sibling, VitalPulse, VitalScope, portal Dashboards

Follow existing project framework and POM conventions. Prefer live probe hosts/titles as SoT; re-probe inside the opened dashboard if dashboard-level export host or Switch metric menu options are still sparse.

After generation:
1. Execute only the newly created Monthly/Quarterly - UX Metrics regression suite and fix automation hard failures.
2. Generate fresh isolated Allure results + HTML report for this suite.
3. Generate manual test-case workbook (Summary / Regression TCs / Notes).
4. Include executed result summary and soft annotations for: empty filter combos, missing Reset/Save As on locked Global, clone cleanup failures, missing PowerPoint option, export download flakes, Switch metric no-op, sparse GDC data, iframe load flakes, GDC re-anchor.

4️⃣ OUTPUT FORMAT
- .ts for Playwright automation / POM / locators
- .html for the fresh Allure report
- .xlsx for manual test cases

5️⃣ CONSTRAINTS — RULES YOU MUST FOLLOW
1. Do NOT generate API or Database test cases.
2. Use existing framework methods whenever possible instead of creating duplicate methods.
3. Strictly follow the POM structure defined in the framework.
4. Use standard TypeScript and Playwright coding practices.
5. Use stable titles/labels and portal `#bi-iframe` frame scope; do not depend on dynamic Highcharts instance IDs or JWT tokens alone.
6. Do not hard-code KPI totals, delta %, axis tick dates, or period subtitle strings from the probe.
7. Capture and restore UX Metrics home, GDC Test Site 2, Comparison Period/End Date/filters baseline; cleanup Save As clones; close export/Switch metric menus.
8. Use a serial suite only when shared page/iframe state is intentional; optional/data-dependent failures must not abort unrelated tests (soft deadlines + recovery).
9. Use bounded soft deadlines and explicit action timeouts.
10. Do not use Promise.race soft deadlines in a way that leaves losing page/iframe actions running into later serial tests; cancel/reload/reset before continuing.
11. Do not use expect.soft inside a soft try/catch when it would still mark a serial test failed; use hard expect inside try/catch and annotate the fallback.
12. Do not Save portal Filters, Clear Genius Cache, permanently edit Global UX Metrics, or leave unrestored filter mutations.
13. Keep primary scope on Monthly/Quarterly - UX Metrics; restore after soft sibling/list/AI Query visits.
14. Close all menus, overlays, drawers, modals, export dialogs opened by tests.
15. Do not claim widgets refreshed by merely counting SVG hosts.
16. Treat timing/%/date units as format-tolerant.
17. Never accept Monthly/Quarterly Revenue, VitalPulse, VitalScope Performance Overview, or CWV Top 10 PoP as suite home without the exact **Monthly/Quarterly - UX Metrics** title in the BI viewer.
18. If any requirement is ambiguous or missing, flag it before generating automation.

6️⃣ AMBIGUITIES TO FLAG BEFORE AUTOMATION
- **Help/PDF** describes the **Period-over-Period Key Metrics** family (8 dashboards across Daily/Weekly/Monthly/Quarterly × Revenue/UX/Bounces/Traffic). Live catalog title for THIS suite is **Monthly/Quarterly - UX Metrics** (combined monthly+quarterly UX view) — treat live exact catalog/viewer title as identity SoT; Help/PDF for purpose/metrics/filters.
- Catalog ~**25 widgets**; live shows mirrored pairs (2× each monthly/quarterly chart block) plus KPI card grids — treat **Onload/LCP/CLS/INP × {Monthly, Quarterly} + Dynamic Chart Metrics** as primary hard inventory; annotate duplicate mirrors.
- Help **Comparison Method** ↔ live **Comparison Period**; Help **Return/New User** ↔ live **Return/New Visitor**; Help **Originated from** ↔ live **Originated From**.
- **Lookback Period** label from other BI dashboards is **not** the primary filter on this viewer (probe: `hasLookbackPeriod: false`); use **Comparison Period + End Date** instead.
- **Apply** button absent on Sep 2026 probe — selection may auto-apply; still assert refresh/signature after change.
- Save As / Reset may be restricted on Global folder dashboards — use disposable clone for mutation depth if needed.
- Dashboard-level top-right hamburger (PNG/PDF/PowerPoint) was **user-required** but not auto-located in Sep 2026 probe — locate during first automation pass (viewer chrome vs Responsive Grid toolbar).
- Per-widget export probe did not open menus — expect **PNG Image / PDF Document / CSV Data** per sibling BI dashboards; soft-search **PowerPoint** at dashboard level only.
- Gear icon live title is **Switch metric** (not “Active metric”) — map user wording to live control.
- Export cross-check is soft/label-level (not pixel-perfect OCR or binary golden files).
- End date “matches a user attribute” is for **scheduled** reports — soft open only; do not leave permanent schedules.
- Outer portal Filters funnel (`#toggle-filters`) is **not** the BI dashboard **Filters** panel.
- Percentile default **75th (p75)** per Help — soft-assert when readable.
- Formal SLA for iframe widget reload / download — use project soft deadlines.
- Coach/Get started overlays may appear on portal shell.
- Parallel workers may race on shared Global filter state — prefer serial for filter / Save As / Switch metric / export tests.

7️⃣ EXAMPLE — MATCH THIS STYLE & DEPTH
Refer to:
- docs/prompts/AI Prompt (For Automation Regression-CWV Top 10 Period over Period PoP).txt
- docs/prompts/AI Prompt (For Automation Regression-Site Operations Dashboard for Page Names).txt
- docs/prompts/AI Prompt (For Automation Regression-Revenue Assurance Dashboard).txt
- docs/prompts/AI Prompt (For Automation Regression-Vital Pulse Dashboard).txt
- pages/CwvTop10PopDashboardPage.ts / SiteOpsPageNamesDashboardPage.ts / VitalPulseDashboardPage.ts (patterns only)

Follow their patterns for POM/locator separation, authenticated GDC site selection, iframe `#bi-iframe` scoping, filter apply/cancel/restore, graph/KPI soft asserts, serial recovery after soft sibling switches, reversible mutations with restore, disposable Save As cleanup, export menu soft cross-checks, isolated Allure + Excel workbooks, and npm naming that does not collide with Monthly/Quarterly Revenue / CWV PoP / VitalPulse / Core Web Vitals.
